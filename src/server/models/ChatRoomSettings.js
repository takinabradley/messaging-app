/* import { Schema, model, ObjectId } from "mongoose"
import Chat from "./Chat"

const ChatRoomSettingsSchema = new Schema({
  chatroom: { type: ObjectId, required: true, ref: Chat }
})

export default model("ChatRoomSettings", ChatRoomSettingsSchema)
 */